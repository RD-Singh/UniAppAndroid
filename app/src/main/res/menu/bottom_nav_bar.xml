<?xml version="1.0" encoding="utf-8"?>
<!-- Creates the different sections in the bottom navigation bar -->
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Creates the home part on the bottom navigation bar -->
    <item
        android:id="@+id/home"
        android:icon="@drawable/ic_university"
        android:title="Home" />

    <!-- Creates the search part on the bottom navigation bar -->
    <item
        android:id="@+id/search"
        android:icon="@drawable/ic_search"
        android:title="Search" />

    <!-- Creates the bookmarks part on the bottom navigation bar -->
    <item
        android:id="@+id/bookmarks"
        android:icon="@drawable/ic_bookmarks"
        android:title="Bookmarks" />

    <!-- Creates the settings part on the bottom navigation bar -->
    <item
        android:id="@+id/settings"
        android:icon="@drawable/ic_settings"
        android:title="Settings" />

</menu>